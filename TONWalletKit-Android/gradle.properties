android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx2g -Dkotlin.daemon.jvm.options=-Xmx2g

# Library Publishing Configuration
GROUP=org.ton
VERSION_NAME=0.0.6
POM_ARTIFACT_ID=walletkit

# Pinned kit tag for the committed WalletKit bridge bundle. CI verifies the bundle matches a
# build of this tag; bump it by hand, then rebuild and recommit the bundle.
WALLETKIT_BRIDGE_TAG=v1.0.0-alpha.3
